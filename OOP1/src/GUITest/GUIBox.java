package GUITest;

import java.util.*;
import javax.swing.*;

public class GUIBox {
	public static void main(String[] args) {
		/*메인에서 실행 할 때
		JFrame box = new JFrame("집들이 나간 공주 구하기");
		//JFrame클래스  변수명  = new 삽입될JFrame클래스("주소줄"); 
		
		
		box.setSize(500, 500);
		//변수.setSize(가로, 세로); 크기 지정
		box.setBackground(null);
		//변수.setBackground(?);
		box.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
		//변수.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);오른쪽 상단 x누르면 종료
		box.setVisible(true);
		//변수.setVisible(boolean값); boolean 값에 따라 창이 보이고 안보이고
		*/
		
		MyFrame aBox = new MyFrame();
		aBox.setSize(300, 300);
		
		//MyFrame bBox = new MyFrame();
		//bBox.setTitle("난 왕자가 좋다");
	}
	
}
